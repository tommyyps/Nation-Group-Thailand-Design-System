{
  "name": "@ntgds/design-system-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Nation Group Thailand Design System — monorepo: แพ็กเกจโทเค็นกลาง + ไซต์เอกสาร (React)",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "tokens:generate": "node scripts/generate-tokens.mjs",
    "dev": "npm run tokens:generate && npm run dev --workspace=@ntgds/docs",
    "build": "npm run tokens:generate && npm run build --workspace=@ntgds/docs",
    "lint": "npm run lint --workspace=@ntgds/docs",
    "preview": "npm run preview --workspace=@ntgds/docs"
  }
}
